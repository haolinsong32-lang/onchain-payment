==============================================
  AI编程效率提示词工具包 · 100条实战Prompt
  兼容 ChatGPT / Claude / Cursor / Copilot
  复制即用 · 终身有效 · 永久更新
==============================================


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【⚡ 代码生成】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

----- 第 1 条 · React + TypeScript 数据展示组件（含加载/错误/空态） -----

【角色】你是一名有6年经验的前端工程师，精通 React 18 + TypeScript，严格遵循函数式组件与 Hooks 最佳实践。

【任务】请用 React 18 + TypeScript 编写一个  组件：
1. 通过 fetch 调用 GET /api/users 获取用户列表，返回 { id:number; name:string; email:string; role:'admin'|'editor'|'viewer' }[]。
2. 处理三种状态：loading（骨架屏）、error（错误信息+"重试"按钮）、empty（"暂无用户"）。
3. 接收可选 pageSize?:number（默认10）做前端分页，底部分页器。
4. 用 AbortController 在卸载时取消未完成请求。
5. role 用不同颜色 Tag 展示。

【输出格式】
- 给出完整 .tsx 代码，命名导出。
- 关键逻辑加中文注释。
- 末尾用 3-5 条要点说明边界处理与父组件用法。

----- 第 2 条 · Node.js Express REST 接口（含参数校验与统一响应） -----

【角色】你是 Node.js 后端工程师，熟悉 Express 4、异步错误处理与接口安全。

【任务】实现 POST /api/articles：
1. 请求体 { title:string; content:string; tags?:string[]; authorId:number }。
2. 用 express-validator 校验：title 2-100字符；content 不少于10字符；tags 每项最长20最多5个；authorId 正整数。
3. 校验失败返回 422，体 { code:422, message:"参数错误", errors:[{field,msg}] }。
4. 通过后调 async articleService.create(data)，成功返回 201 { code:0, data }。
5. try/catch 包裹，异常交 next(error)。
6. 路由只调 service，不内联数据库逻辑。

【输出格式】
- 给出路由文件、控制器片段、service 接口签名。
- 列出 curl 示例（一个非法、一个合法）。

----- 第 3 条 · Python FastAPI CRUD（Pydantic v2 + 依赖注入） -----

【角色】你是 Python 后端工程师，精通 FastAPI、Pydantic v2 与 SQLAlchemy 2.0。

【任务】用 FastAPI 编写 Todo 完整 CRUD：
1. 模型 Todo：id 主键自增、title 必填最大120、done 默认False、created_at 自动生成。
2. Pydantic v2 定义 TodoCreate/TodoUpdate/TodoOut，created_at 输出 ISO8601。
3. 五接口：POST/GET列表(分页+done过滤)/GET{id}/PUT/DELETE。
4. Depends 注入数据库会话，查不到抛 404。
5. 所有接口带中文 summary 与 response_model。

【输出格式】
- 按 models.py/schemas.py/crud.py/main.py 分别给出。
- 附 uvicorn 启动命令，说明内存实现与真实数据库替换点。

----- 第 4 条 · Vue 3 Composition API 组件 + Pinia 状态管理 -----

【角色】你是 Vue 3 高级前端，熟悉 script setup、组合式函数与 Pinia。

【任务】实现购物车：
1. Pinia useCartStore：state items:{id,name,price,qty}[]。
2. getters：totalCount、totalPrice(两位小数)、isEmpty。
3. actions：addItem(已存在则qty+1)、removeItem、updateQty、clear。
4.  显示 totalCount 点击跳转 /cart。
5.  展示列表、加减按钮、清空、总价；数量0自动移除。
6. 全部用 script setup lang="ts"，store 用 setup 风格。

【输出格式】
- 分别给 cartStore.ts、CartBadge.vue、CartPage.vue。
- 说明 main.ts 注册 Pinia。

----- 第 5 条 · 自定义 Hook：防抖搜索 + AbortController 竞态取消 -----

【角色】你是 React 性能优化专家，擅长自定义 Hooks 与请求竞态处理。

【任务】编写 useDebounceSearch(fetcher, delay=300)：
1. fetcher: (keyword:string, signal:AbortSignal) => Promise。
2. 输入变化延迟 delay 发请求；新输入取消上一次请求。
3. 必须用 AbortController 真正取消，而非忽略过期响应。
4. 返回 { data, loading, error, keyword, setKeyword }。
5. 卸载时取消进行中请求。
6. 示例：搜索 GitHub 仓库，展示前10条。

【输出格式】
- 先给 useDebounceSearch.ts 完整实现。
- 再给 SearchRepos.tsx 示例。
- 说明竞态根因与保证最后一次结果显示的机制。

----- 第 6 条 · Express JWT 认证中间件（刷新令牌+Redis黑名单） -----

【角色】你是后端安全工程师，熟悉 JWT、令牌轮换与鉴权。

【任务】实现 Express 认证：
1. authenticate：从 Bearer token 读 access token，jsonwebtoken 验证，失败401；通过挂 req.user={userId,role}。
2. authorize(...roles) 工厂，角色不符403。
3. POST /auth/refresh：refresh 换新 access，旧 refresh 加入 Redis 黑名单，TTL 为剩余有效期。
4. access 15分钟，refresh 7天。

【输出格式】
- 给 auth.middleware.js、auth.controller.js、redis 黑名单工具。
- 给受保护路由示例。
- 列出需读取的环境变量。

----- 第 7 条 · Python 数据库事务上下文管理器 -----

【角色】你是 Python 工程师，熟悉 contextlib 与事务管理。

【任务】用 @contextmanager 实现 transaction(get_conn)：
1. 进入获取连接+游标，yield 游标。
2. 无异常 commit；异常 rollback 并向上抛。
3. finally 关闭游标、归还连接。
4. 示例：事务中同时插 orders 与 order_items。
5. 额外给 sqlite3 版本方便本地测试。

【输出格式】
- 给 db.py（psycopg2 与 sqlite3 两版）。
- 给 transfer 函数示例(A扣B加原子)。
- 说明 READ COMMITTED 与 REPEATABLE READ 选择。

----- 第 8 条 · TypeScript 泛型 API 客户端（拦截器+统一错误） -----

【角色】你是 TypeScript 架构工程师，擅长类型安全 HTTP 封装。

【任务】封装 ApiClient 类（基于 fetch）：
1. 构造 { baseURL, timeout, getToken }。
2. get/post/put/patch/delete 泛型方法，自动 JSON。
3. 请求拦截加 Content-Type 与 Authorization。
4. 非2xx 抛 ApiError(status,url,message)；超时抛 TimeoutError；网络错误抛 NetworkError。
5. AbortController+setTimeout 实现超时。

【输出格式】
- 给 apiClient.ts 含三个错误类。
- 给 api/modules/user.ts 使用示例。
- 对比 axios 说明 fetch 在取消与进度事件的差异。

----- 第 9 条 · Node.js WebSocket 聊天服务器（房间+心跳） -----

【角色】你是实时通信工程师，熟悉 ws 库与连接管理。

【任务】用 ws 实现聊天服务器：
1. 连接后须先发 {type:'auth',token}，10秒未鉴权断开。
2. join/leave 房间；message 广播给房间其他人（加 from/timestamp）。
3. 心跳：服务端30秒 ping，60秒无 pong 断开。
4. 内存维护房间→连接集合，断开自动清理。
5. 单条消息超4KB 关闭连接(1009)。

【输出格式】
- 给 server.js 完整代码。
- 给浏览器端最小示例。
- 列生产环境3项补充(Redis pub/sub等)。

----- 第 10 条 · Python pandas 链式数据清洗管道 -----

【角色】你是数据工程师，精通 pandas 与可复现管道。

【任务】用 pipe 链式清洗销售 CSV(列:order_id,customer_name,phone,amount,order_time,status)：
1. 每步独立函数：去重、姓名trim+格式、phone校验11位(非法NaN)、amount转数值(负数NaN)、order_time解析(失败丢弃)、status只保留paid/shipped/cancelled。
2. 返回清洗后 DataFrame + 报告{原始行数,去重后行数,各字段缺失数,丢弃行数}。
3. 不修改原 DataFrame。

【输出格式】
- 给 clean_sales.py，每函数带 docstring。
- 给 __main__ 读取→清洗→打印报告→导出示例。
- 说明 pipe 链式两个优点。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【🐛 Bug调试】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

----- 第 11 条 · React useEffect 无限循环定位与修复 -----

【角色】你是 React 调试专家，擅长 Hooks 依赖问题。

【任务】分析以下代码为何无限循环并修复：
function Profile({ userId }) {
  const [user, setUser] = useState(null);
  const fetchUser = () => fetch('/api/users/'+userId).then(r=>r.json()).then(setUser);
  useEffect(() => { fetchUser(); }, [fetchUser]);
  return {user?.name};
}
1. 解释完整触发链。
2. 两种修复：useCallback 包裹 fetchUser；逻辑直接放 useEffect 用 userId 依赖。
3. 说明 fetchUser 需复用时哪种更合适。
4. userId 为 undefined 时不发请求。

【输出格式】
- 分步解释根因。
- 给两种修复后完整组件。
- 3条 checklist 避免依赖死循环。

----- 第 12 条 · Node.js EventEmitter 内存泄漏警告排查 -----

【角色】你是 Node.js 排障工程师。

【任务】日志出现 MaxListenersExceededWarning: 11 message listeners added to [process]：
1. 解释警告确切含义（为何通常意味 bug 而非调大上限）。
2. 列4种成因（每请求 process.on 不 off、循环添加、闭包持有大对象、重复初始化单例）。
3. 定位：--trace-warnings、console.trace、_getActiveHandles()。
4. 修复模板：确保只注册一次或适时 off。

【输出格式】
- 编号列表成因+反例代码。
- 给"安全单例事件注册"工具函数。
- 说明何时才需 setMaxListeners(0)。

----- 第 13 条 · Python asyncio 竞态导致余额为负 -----

【角色】你是 Python 异步专家，熟悉 asyncio 竞态。

【任务】以下代码高并发下余额为负/重复扣款：
async def withdraw(account, amount):
    bal = await get_balance(account)
    if bal >= amount:
        await asyncio.sleep(0.01)
        await set_balance(account, bal - amount)
        return True
    return False
1. 解释 await 挂起导致竞态（协作式并发）。
2. 两种修复：asyncio.Lock 串行化；数据库乐观锁 UPDATE...WHERE balance>=amount 据 rowcount 判断。
3. 对比适用场景与性能。
4. 给 gather 并发100次的验证脚本。

【输出格式】
- 文字说明竞态时间线。
- Lock 版与乐观锁版代码。
- 验证脚本与预期结果。

----- 第 14 条 · TypeScript 类型收窄不生效排查 -----

【角色】你是 TS 类型工程师，熟悉控制流分析。

【任务】解释为何 value.toFixed(2) 报 possibly null，filter 后 str.toUpperCase 报 string|undefined：
function format(value: number | null) {
  if (value == null) return '0.00';
  const fn = () => value.toFixed(2);
  return fn();
}
const arr:(string|undefined)[]=['a',undefined,'b'];
const upper=arr.filter(s=>s!=null).map(s=>s.toUpperCase());
1. 闭包内收窄失效原因（TS 不对闭包可变变量窄化）。
2. filter 谓词需类型谓词。
3. 修复：const 局部不可变；is 类型守卫。
4. 给可辨识联合收窄示例。

【输出格式】
- 每问题先原因后修复。
- 给 notEmpty 类型守卫。
- 3条收窄常见误区。

----- 第 15 条 · 前端请求竞态：旧响应覆盖新结果 -----

【角色】你是前端异步调试专家。

【任务】快速输入 a→ab→abc，最终显示 a 的结果：
1. 解释竞态条件（返回顺序≠发起顺序）。
2. 三种修复：请求序号比对忽略过期；AbortController 取消上次；RxJS switchMap/SWR/react-query。
3. 对比优缺点（是否真取消、服务端压力、复杂度）。
4. 给 React+fetch 最小复现与 AbortController 修复。

【输出格式】
- 文字时序图说明。
- 请求序号版与 AbortController 版代码。
- 推荐方案与理由。

----- 第 16 条 · CSS Flexbox 内容溢出撑破容器 -----

【角色】你是 CSS 布局专家。

【任务】flex 容器内长文本撑破布局：

  
  长文本aaaaa...
  操作

1. 解释 flex 子项默认 min-width:auto（最小尺寸=内容固有尺寸）。
2. 修复：.content 设 min-width:0。
3. 长单词用 word-break/overflow-wrap。
4. 多行截断 -webkit-line-clamp 写法与 flex 中生效前提。

【输出格式】
- 解释 min-size 算法。
- 修复后完整 HTML/CSS。
- 3条溢出排查清单。

----- 第 17 条 · MySQL 死锁排查与修复 -----

【角色】你是数据库性能工程师，熟悉 InnoDB 锁。

【任务】偶发 Deadlock found when trying to get lock：
1. 解释死锁四必要条件在数据库的体现。
2. 排查：SHOW ENGINE INNODB STATUS 的 LATEST DETECTED DEADLOCK 段解读。
3. 5种成因（不同顺序更新、缺索引致锁升级、长事务、热点行、CASCADE 链式加锁）。
4. 修复：统一加锁顺序、缩短事务、加索引、降隔离级别、应用层重试。

【输出格式】
- 模拟死锁日志解读。
- 按主键升序更新代码示例。
- 指数退避重试装饰器。

----- 第 18 条 · CORS 跨域错误完整排查 -----

【角色】你是全栈工程师，精通同源策略与 CORS。

【任务】前端报 No 'Access-Control-Allow-Origin' header：
1. 区分简单请求与预检(OPTIONS)，application/json 或自定义头触发预检。
2. 服务端须返回：Allow-Origin(带credentials不能为*)、Allow-Methods、Allow-Headers、Allow-Credentials、Max-Age。
3. 常见错误：*又带cookie、只处理GET没处理OPTIONS、自定义头未列、HTTPS/HTTP/端口不同源。
4. 给 Nginx 与 Express 两套配置。

【输出格式】
- 决策树排查步骤。
- Nginx 片段与 Express cors 配置。
- 解释带cookie时为何必须回显具体origin。

----- 第 19 条 · UnhandledPromiseRejection 崩溃定位 -----

【角色】你是 Node.js 稳定性工程师。

【任务】Node 18+ 因 unhandledRejection 退出：
1. 解释新版默认 --unhandled-rejections=throw 行为。
2. 定位：unhandledRejection 事件打印堆栈、--trace-warnings、补 await/.catch。
3. 4种场景：forEach+async、漏 await、Promise.all 某失败、事件回调 async 抛错。
4. 进程级兜底（日志、优雅关闭），不建议 process.exit(0)。

【输出格式】
- 给 registerGlobalHandlers() 函数。
- forEach+async 反例改 for...of。
- 3条编码规范。

----- 第 20 条 · 浮点数精度 0.1+0.2≠0.3 -----

【角色】你是熟悉 IEEE 754 的工程师。

【任务】金额计算出现 0.1+0.2=0.30000000000000004：
1. 解释 IEEE 754 双精度无法精确表示0.1/0.2。
2. 三种修复：整数"分"存储计算(推荐)；decimal.js/big.js；展示用 toFixed(注意银行家舍入)。
3. 给 addMoney(a,b) 以分为单位。
4. 给 floatEqual(a,b,epsilon=1e-9) 容差比较。

【输出格式】
- 简短解释二进制浮点。
- 分单位 TS 实现(元转分/分转元)。
- decimal.js 示例与 floatEqual。
- 明确推荐方案(a)。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【🔧 代码重构】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

----- 第 21 条 · 拆分超长函数(>100行)为可测试小函数 -----

【角色】你是重构专家，遵循单一职责。

【任务】重构约120行的 processOrder（校验/查库存/扣库存/算价/优惠/建单/通知/日志）：
1. 拆为 validateOrderData、checkInventory、reserveInventory、calculatePrice、applyDiscount、createOrder、notifyCustomer、logOrder。
2. 主函数只编排，控制20行内。
3. 每小函数参数返回明确，不依赖共享可变变量，错误用自定义异常。
4. 行为不变，给前后调用对比。
5. 指出可独立单测的函数。

【输出格式】
- 给重构后完整 TS 代码。
- 4条收益要点。
- 说明如何用契约测试保证行为不变。

----- 第 22 条 · 策略模式替换冗长 switch -----

【角色】你是设计模式专家，熟悉开闭原则。

【任务】重构支付代码（每加支付方式改主函数）：
function pay(type, amount) {
  switch(type) {
    case 'alipay': return alipayPay(amount);
    case 'wechat': return wechatPay(amount);
    case 'card': return cardPay(amount);
  }
}
1. 定义 PayStrategy 接口 { pay(amount):Promise }，每方式实现策略类。
2. Map/注册表注册，pay 从注册表取。
3. 新增方式只加文件不改 pay（开闭原则）。
4. 未知 type 抛有意义错误。
5. 给新增 applepay 示例证明不改 pay。

【输出格式】
- 接口/策略类/注册表/pay 函数。
- applepay 新增代码。
- 对比 switch 说明代价与何时不值得。

----- 第 23 条 · 消除跨文件重复代码(DRY) -----

【角色】你是注重复用且警惕过度抽象的工程师。

【任务】user.service 与 order.service 各有约30行相同"分页+总数+脱敏"逻辑：
1. 先判断是否真该合并（真正相同 vs 语义偶合未来分化）。
2. 提取泛型 paginate(query,{page,size,sanitize})。
3. sanitize 作注入钩子，各 service 保留脱敏规则。
4. 两 service 改调工具。
5. 建议三振法则（第三次重复才提取）。

【输出格式】
- 给 paginate.ts 泛型实现。
- 给 user.service 重构片段。
- 说明何种重复不该合并。

----- 第 24 条 · 回调地狱重写为 async/await -----

【角色】你是 Node.js 异步重构专家。

【任务】重写回调地狱：
getUser(userId, (err, user) => {
  if(err) return done(err);
  getOrders(user.id, (err, orders) => {
    if(err) return done(err);
    getItems(orders[0].id, (err, items) => { if(err) return done(err); done(null,{user,orders,items}); });
  });
});
1. promisify 后 async/await 线性流程。
2. try/catch 统一错误。
3. getOrders 与 getItems 无依赖则 Promise.all 并行。
4. 非 error-first 回调手动 promisify。

【输出格式】
- promisify 后函数与 async 主函数。
- Promise.all 并行版。
- 说明 async/await 堆栈调试优势。

----- 第 25 条 · 魔法数字/字符串替换为常量枚举 -----

【角色】你是注重可维护性的工程师。

【任务】重构散落魔法值：
if (user.status === 1) {...}
if (role === 'admin') {...}
setTimeout(retry, 3000);
1. 状态码提取 enum UserStatus { ACTIVE=1, DISABLED=2, PENDING=3 }。
2. 角色提取 const ROLES。
3. 超时/重试次数提取语义常量集中 constants.ts。
4. 不为自解释单次数字强行命名。
5. 前后对比。

【输出格式】
- constants.ts 与 enum。
- 重构后业务片段。
- 3条判断准则。

----- 第 26 条 · 拆解上帝类(God Class) -----

【角色】你是 OO 重构专家。

【任务】OrderService 3000行同时负责校验/计价/库存/支付/通知/日志/导出：
1. 拆为 OrderValidator、PriceCalculator、InventoryService、PaymentService、NotificationService、OrderExporter。
2. OrderService 作门面(Facade)保留，构造注入协作者编排。
3. 每类职责单一可独立测试。
4. 构造注入不内部 new 便于 mock。
5. 前后调用对比。

【输出格式】
- 各类 TS 骨架(类名/方法签名/依赖)。
- 门面 placeOrder 编排代码。
- 说明门面降低心智负担。

----- 第 27 条 · 嵌套条件改早返回(卫语句) -----

【角色】你是可读性专家，推崇扁平结构。

【任务】重构深层嵌套：
function getDiscount(user, order) {
  if (user) { if (user.isVip) { if (order) { if (order.amount > 1000) { return 0.2; } else { return 0.1; } } } else { return 0; } }
}
1. 卫语句提前 return 无效/边界情况，消除 else。
2. 主逻辑保持最左缩进。
3. 补 null 防御。
4. 再给权限校验示例（未登录401/无角色403/无资源404/否则返回数据）。

【输出格式】
- 重构后 getDiscount。
- 权限校验示例。
- 卫语句适用场景与注意点。

----- 第 28 条 · React 类组件迁移函数组件+Hooks -----

【角色】你是 React 现代化迁移专家。

【任务】迁移类组件：
class Counter extends React.Component {
  constructor(props){super(props);this.state={count:0};this.inc=this.inc.bind(this);}
  componentDidMount(){document.title='count:'+this.state.count;}
  componentDidUpdate(){document.title='count:'+this.state.count;}
  inc(){this.setState(s=>({count:s.count+1}));}
  render(){return {this.state.count};}
}
1. useState+useEffect。
2. 移除 bind/constructor。
3. didMount+didUpdate 合并一个 useEffect。
4. shouldComponentUpdate 对应 React.memo+useMemo/useCallback。

【输出格式】
- 函数组件完整代码。
- 生命周期到 Hooks 对照表。
- 2个易遗漏点。

----- 第 29 条 · 引入参数对象收拢长参数列表 -----

【角色】你是 API 设计专家。

【任务】createUser(name,email,phone,age,role,department,hireDate,address) 8参数易传错：
1. 收拢为 CreateUserParams 接口，字段带类型注释。
2. 签名改 createUser(params: CreateUserParams)。
3. 可选字段默认值(role 默认 viewer)。
4. 前后调用对比（位置参数 vs 具名）。
5. 超3参数且同概念建议采用。

【输出格式】
- interface 与重构后函数。
- 调用对比。
- 两个额外好处。

----- 第 30 条 · 组合替代继承 -----

【角色】你是熟悉"组合优于继承"的架构师。

【任务】BaseRepository 被子类继承，混入缓存/日志/软删除，子类继承不需要的方法：
1. 拆为 withCache、withLogger、withSoftDelete 独立能力。
2. Repository 只留数据访问，按需组合（构造注入/高阶函数/装饰器）。
3. UserRepository 组合缓存+日志；OrderRepository 组合软删除+日志。
4. 对比继承解决"继承爆炸"。

【输出格式】
- 三能力 TS 实现(高阶函数/mixin)。
- 两 Repository 组合代码。
- 何时仍适合继承。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【✅ 单元测试】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

----- 第 31 条 · Jest 工具函数测试(边界与异常) -----

【角色】你是测试工程师，信奉测试即文档。

【任务】为 formatCurrency(amount,currency='CNY') 写 Jest 测试：
1. 正数/0/负数(-¥1,234.50)。
2. 四舍五入到分(1.005→¥1.01)。
3. 千分位(1234567.89→¥1,234,567.89)。
4. USD 用 $。
5. null/undefined/NaN/非数字字符串抛 TypeError。
6. describe/it 分组，用例"应该..."中文命名。

【输出格式】
- 给实现与 test，用 it.each 参数化。
- 说明哪些是边界值分析。

----- 第 32 条 · React Testing Library 组件行为测试 -----

【角色】你是前端测试工程师，以用户方式测试。

【任务】测 （输入框+添加按钮）：
1. 初始空、按钮禁用。
2. 输入后可点；点击调 onAdd(text) 并清空。
3. Enter 触发添加。
4. 纯空格不触发(trim)。
5. 添加后输入框重获焦点。
6. 用 getByRole/fireEvent/userEvent，不测内部 state。

【输出格式】
- 给 TodoInput.tsx 与 .test.tsx。
- 用 user-event 而非 fireEvent。
- 说明 getByRole 优势。

----- 第 33 条 · Mock API 与模块依赖 -----

【角色】你是前端测试工程师，擅长隔离依赖。

【任务】测 login(username,password) 调 axios：
1. jest.mock('axios') 模拟 post，测成功(200+token)与失败(401)。
2. 不发真实请求。
3. 断言以正确 URL/参数调用。
4. 失败断言错误信息。
5. 给 MSW 拦截 fetch 对比示例，说明更贴近网络层。

【输出格式】
- auth.js 与 .test.js(jest.mock)。
- MSW handler 与 setup。
- 对比适用场景。

----- 第 34 条 · 异步与定时器测试(假定时器) -----

【角色】你是 Jest 高级用户。

【任务】测 debounce(fn, delay)：
1. jest.useFakeTimers() 替换定时器。
2. 连续触发3次，delay 后只执行1次。
3. advanceTimersByTime(300) 快进。
4. 测 leading/trailing。
5. 测 cancel()。
6. 结束 useRealTimers() 恢复。

【输出格式】
- debounce.ts 与 .test.ts。
- 演示 act() 包裹。
- 说明 fake timers 解决慢与不确定。

----- 第 35 条 · Pytest + fixture + 参数化 -----

【角色】你是 Python 测试工程师。

【任务】为 is_valid_email(email) 写 pytest：
1. 合法：user@example.com、a.b@sub.co.cn、user+tag@domain.io。
2. 非法：缺@、@开头、多@、无域名、含空格、空串、None(抛TypeError)。
3. @pytest.mark.parametrize 参数化。
4. fixture 提供临时邮箱列表(conftest.py)。
5. pytest.raises 断言 None 抛 TypeError。

【输出格式】
- validator.py/test_validator.py/conftest.py。
- 参数化分组。
- 说明 fixture 优势。

----- 第 36 条 · Redux reducer 纯函数测试 -----

【角色】你是状态管理测试工程师。

【任务】测 todo reducer(ADD_TODO/TOGGLE_TODO/DELETE_TODO/SET_FILTER)：
1. 每用例验证：初始状态、派发后新状态、不修改原状态(不可变)。
2. toBe 检查引用变化，toEqual 检查值。
3. 未知 action 返回原状态。
4. 串联多 action 测集成。

【输出格式】
- todosSlice.ts(RTK) 与 .test.ts。
- 验证未突变。
- 说明 reducer 测试快且稳定。

----- 第 37 条 · 快照测试 vs 行为测试取舍 -----

【角色】你是务实测试架构师。

【任务】为  设计测试策略：
1. 行为测试：点击触发 onClick、disabled 不触发、aria-label 正确。
2. 快照测试：不同 variant/size 组合。
3. 说明快照优点与陷阱(throwaway、随意-u掩盖问题)。
4. 何时用快照何时用断言。
5. 行为用 RTL 显式断言，快照只对稳定展示型组件。

【输出格式】
- Button.test.tsx 含行为与快照。
- 3条快照最佳实践。
- 明确交互逻辑必须行为测试。

----- 第 38 条 · 自定义 React Hook 测试 -----

【角色】你是 Hooks 测试专家。

【任务】测 useToggle(initial=false) 返回 [value,toggle,setTrue,setFalse]：
1. renderHook + act。
2. 测初始值/toggle/setTrue/setFalse。
3. initial=true 初始值正确。
4. 状态更新包 act()。
5. 若 Hook 用 useEffect 订阅，unmount 时验证清理调用。

【输出格式】
- useToggle.ts 与 .test.ts。
- 带订阅的 Hook 测试示例(jest.fn 断言清理)。
- 说明 result.current 访问模式。

----- 第 39 条 · Express 中间件与接口测试(supertest) -----

【角色】你是后端测试工程师。

【任务】用 jest+supertest 测 Express：
1. GET /api/users 返回200与数组。
2. POST 缺 name 返回422。
3. GET /api/users/9999 返回404。
4. mongodb-memory-server/sqlite memory 作测试库。
5. 每测试前清空数据。
6. 鉴权中间件：无token 401，有效token 200。

【输出格式】
- app.js(导出app) 与 .api.test.js。
- 内存库 setup/teardown。
- 说明接口测试更能发现集成问题。

----- 第 40 条 · 参数化/表驱动测试 -----

【角色】你是追求覆盖率与简洁的工程师。

【任务】表驱动测 validatePassword(pwd)（长度≥8、含大小写、数字、特殊字符）：
1. 数组定义 {pwd,expected,reason}。
2. 覆盖：全满足、缺大写、长度不足、空串、仅数字。
3. Jest it.each 版与 pytest parametrize 版。
4. 断言信息含 reason。
5. 新增场景只加一行数据。

【输出格式】
- validatePassword 实现。
- .test.js(it.each) 与 test_password.py(parametrize)。
- 3个优点。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【🔍 代码审查】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

----- 第 41 条 · 安全审查清单(注入/认证/敏感信息) -----

【角色】你是应用安全工程师，攻击者视角。

【任务】审查登录与订单查询代码：
1. SQL注入：是否拼接，应参数化。
2. XSS：用户输入未转义渲染。
3. 认证：密码 bcrypt/argon2(禁明文/MD5)、JWT密钥硬编码、cookie httpOnly/secure/sameSite。
4. 越权：查订单校验 order.userId===req.user.userId(IDOR)。
5. 敏感信息：日志打印密码/token、错误泄露堆栈。
6. 依赖漏洞(npm audit/pip-audit)。
7. 登录限流防爆破。

【输出格式】
- 表格：序号|风险项|严重程度|位置|修复建议。
- 高危项给修复代码。
- 3条 CI 安全检查。

----- 第 42 条 · SOLID 原则违规审查 -----

【角色】你是 OO 设计审查专家。

【任务】对照 SOLID 审查代码：
1. SRP：类/函数是否做多于一件事。
2. OCP：新增类型是否须改已有函数(switch扩张)。
3. LSP：子类是否改变父类语义或抛未声明异常。
4. ISP：胖接口强迫实现不需要方法。
5. DIP：高层是否直接依赖具体实现(业务类里 new MySQLConnection)。
用依赖注入/策略/接口拆分修正。

【输出格式】
- S/O/L/I/D 各给违规点+修复代码。
- 每条一句解释。
- 一句话总结核心目标。

----- 第 43 条 · 错误处理审查(空catch/吞错/粒度) -----

【角色】你是稳定性审查工程师。

【任务】审查异步代码错误处理：
1. 空 catch(e){} 吞错。
2. catch 只 console.log 不抛不降级。
3. catch 过宽掩盖编程bug。
4. throw 'error' 字符串无堆栈。
5. 未区分可恢复(校验失败)与不可恢复(DB连不上)。
6. async 漏 await 致 rejection 无人处理。
给自定义错误类/错误边界/统一日志/降级重试。

【输出格式】
- 问题清单(编号+反例)。
- 重构代码(含 AppError 分层)。
- 错误处理三问。

----- 第 44 条 · 异步代码审查(async/await 常见坑) -----

【角色】你是异步代码审查专家。

【任务】审查 async/await 代码：
1. 串行 await 无依赖请求(应 Promise.all)。
2. forEach 中 async(不等待，应 for...of/Promise.all)。
3. 漏 await 致未捕获 rejection。
4. Promise.all 一败全败(需 allSettled/单独catch)。
5. 无超时可能永久挂起。
6. 循环逐条查库(N+1，应批量)。
修复并对比性能。

【输出格式】
- 每问题反例+修复。
- all vs allSettled 建议。
- withTimeout(promise,ms) 工具。

----- 第 45 条 · 命名与可读性审查 -----

【角色】你是可读性审查者。

【任务】审查代码命名：
1. 变量名是否表达意图(data/info/temp/obj/res 替换)。
2. 布尔用 is/has/can/should 前缀。
3. 函数名动词开头且与返回/副作用一致(getX 不发请求，fetchX 才异步)。
4. 误导性缩写(usr/cnt/tmp)。
5. 可搜索(避免单字母除循环计数)。
6. 同一概念前后一致(user/customer/account)。

【输出格式】
- 表格：原名|新名|理由。
- 重命名后片段。
- 3条命名原则。

----- 第 46 条 · 注入与 XSS 专项审查 -----

【角色】你是 Web 安全审查员，专注 OWASP。

【任务】审查"用户评论+展示"功能：
1. SQL注入：参数化。
2. XSS：dangerouslySetInnerHTML/v-html 直接渲染；富文本用 DOMPurify 消毒。
3. CSRF：token 或 SameSite。
4. 限流。
5. 内容长度/类型校验。
6. CSP 头降低 XSS 影响。

【输出格式】
- 漏洞清单与修复代码。
- DOMPurify 示例与 CSP(Nginx/Helmet)。
- 存储型 vs 反射型 XSS 区别。

----- 第 47 条 · 第三方依赖审查 -----

【角色】你是供应链安全工程师。

【任务】审查项目依赖：
1. npm audit/pip-audit 查 CVE，区分运行时与 dev。
2. 无人维护包(发布时间/issues)。
3. 包体积过大(只用到一个函数的 lodash 整体引入)。
4. 依赖混淆风险(内部包名被抢注)。
5. 版本锁定(lock 文件)。
6. 可疑 postinstall 脚本。

【输出格式】
- 报告模板(依赖|版本|风险|建议)。
- 按需引入 lodash 示例。
- CI 加 npm audit --audit-level=high。

----- 第 48 条 · 可访问性(A11y)审查 -----

【角色】你是前端可访问性审查员，熟悉 WCAG 2.1。

【任务】审查表单页面：
1. 控件关联 label(htmlFor/aria-label)。
2. 图片 alt(装饰性 alt="")。
3. 按钮有可识别名称。
4. 颜色对比度(正文4.5:1)。
5. 键盘可操作，焦点可见顺序合理。
6. 错误用 aria-invalid/aria-describedby。
7. 动态内容用 aria-live。

【输出格式】
- 问题清单与严重程度。
- 修复后表单 JSX。
- 推荐 axe/eslint-plugin-jsx-a11y。

----- 第 49 条 · Pull Request Diff 结构化审查 -----

【角色】你是 Tech Lead，负责高质量 CR。

【任务】给 PR 审查标准化流程与模板：
1. 目的(对应 issue)。2. 正确性(边界/错误)。3. 安全(注入/越权/泄露)。
4. 可维护性(命名/长度/重复/复杂度)。5. 测试(有无/覆盖/稳定)。
6. 性能(N+1/重渲染/大对象)。7. 兼容性(API/schema/迁移)。
评论区分 Blocker/Suggestion/Question/Praise。

【输出格式】
- Markdown 审查意见模板。
- 建设性评论示例(问题+原因+代码建议)。
- CR 首要目标是质量与知识共享。

----- 第 50 条 · 性能反模式审查 -----

【角色】你是性能审查工程师。

【任务】审查代码找性能反模式：
1. 前端：render 调昂贵函数、内联对象 prop 致重渲染、缺 key/index 作 key、未虚拟化、未懒加载。
2. 后端：N+1、循环查库/调远程、缺索引、同步IO阻塞、未分页全表、未缓存。
3. 通用：重复计算未缓存、大对象深拷贝、循环重复编译正则、同步写日志。

【输出格式】
- 分类列反模式/问题代码/修复代码。
- DataLoader 或批量查询修 N+1 示例。
- useMemo/useCallback 合理使用判断。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【🚀 性能优化】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

----- 第 51 条 · React 重渲染优化(memo/useMemo/useCallback) -----

【角色】你是 React 性能优化专家。

【任务】列表页输入时所有  都重渲染：
1. 用 DevTools Profiler 定位（Highlight updates）。
2. React.memo 包裹 ListItem，useMemo 缓存计算，useCallback 稳定回调。
3. 内联对象/数组 prop 会让 memo 失效，需提升或 useMemo。
4. 状态下放：输入框 state 下移独立组件。
5. 给前后代码与 Profiler 对比。
6. 不要无脑 memo，先 Profiler 验证。

【输出格式】
- 优化前后组件代码。
- 4个 memo 失效原因。
- 何时该用 useMemo/useCallback 判断标准。

----- 第 52 条 · 慢 SQL 分析与索引优化(EXPLAIN) -----

【角色】你是数据库性能工程师，精通 EXPLAIN。

【任务】SQL 耗时3秒：
SELECT * FROM orders WHERE customer_id=? AND status='paid' ORDER BY created_at DESC LIMIT 20;
1. EXPLAIN 分析：type 是否 ALL、key 是否命中、rows 扫描行数、Extra 是否 filesort/temporary。
2. 设计复合索引 (customer_id,status,created_at)，说明最左前缀与排序字段顺序。
3. 避免 SELECT *。
4. EXPLAIN 验证 type=ref、无 filesort。
5. 说明回表与覆盖索引。

【输出格式】
- 优化前 EXPLAIN 解读(模拟)。
- 建索引 SQL 与优化后 EXPLAIN。
- 复合索引顺序3条经验。

----- 第 53 条 · 修复 N+1 查询 -----

【角色】你是 ORM 与数据库性能工程师。

【任务】循环查每用户订单，100用户产生101条SQL：
users.forEach(u => { u.orders = await db.query('SELECT * FROM orders WHERE user_id=?',[u.id]); });
1. 指出 N+1 对连接池与延迟影响。
2. 修复：先查用户，再 WHERE user_id IN(...) 一次查订单，内存分组组装。
3. ORM 方案：Prisma include、Django select_related/prefetch_related、TypeORM relations。
4. IN 过长则分批(500)或游标。

【输出格式】
- 原生批量查询+内存组装(Node/Python)。
- Prisma include 示例。
- 如何用日志识别 N+1。

----- 第 54 条 · 长列表虚拟化(虚拟滚动) -----

【角色】你是前端渲染性能工程师。

【任务】展示10000条数据页面卡死：
1. 解释 DOM 节点过多的布局/绘制/内存开销。
2. 虚拟滚动：只渲染可视区+缓冲，transform:translateY 定位。
3. 基于 react-window（FixedSizeList 与 VariableSizeList）。
4. scrollToItem 滚动到指定项。
5. 不引库则给最简原理实现（计算 startIndex/endIndex、总高占位）。

【输出格式】
- react-window 示例。
- 原生 JS 核心实现(约50行)。
- 两个注意点(行高测量、搜索定位)。

----- 第 55 条 · 图片懒加载与现代格式 -----

【角色】你是前端性能工程师，关注 Core Web Vitals。

【任务】优化含大量图片的列表页：
1. loading="lazy" decoding="async" 原生懒加载。
2. IntersectionObserver 自定义懒加载，rootMargin 提前加载。
3. WebP/AVIF 格式，+ 回退 JPEG。
4. srcset/sizes 响应式图片。
5. width/height 或 aspect-ratio 防 CLS。
6. LCP 图片不懒加载，preload/fetchpriority="high"。

【输出格式】
- 完整 +srcset 示例。
- IntersectionObserver 懒加载组件。
- LCP 优化3要点。

----- 第 56 条 · 打包体积分析与代码分割 -----

【角色】你是前端构建与产物体积工程师。

【任务】首屏 bundle 2MB：
1. webpack-bundle-analyzer/rollup-plugin-visualizer 分析。
2. 路由级分割：React.lazy+Suspense。
3. 大依赖按需引入或替换（moment→dayjs、lodash-es、echarts tree-shaking）。
4. Tree-shaking：ESM import、sideEffects 配置。
5. 动态 import 大组件。
6. gzip/brotli、contenthash 长期缓存。

【输出格式】
- React.lazy 路由分割代码。
- dayjs 替换与 lodash 按需示例。
- 5个手段按收益排序。

----- 第 57 条 · 缓存策略(HTTP/CDN/应用层) -----

【角色】你是缓存架构工程师。

【任务】为内容型网站设计多级缓存：
1. HTTP：静态资源 Cache-Control:public,max-age=31536000,immutable+内容哈希；HTML no-cache+ETag。
2. CDN 缓存图片/静态资源。
3. 应用层 Redis 缓存热点，TTL，更新失效(cache-aside)。
4. 防击穿/穿透/雪崩：互斥锁、空值缓存、TTL 随机抖动。
5. "先更新DB再删缓存"及原因。

【输出格式】
- Nginx 静态缓存配置片段。
- Redis cache-aside 工具函数。
- 击穿/穿透/雪崩分别解释。

----- 第 58 条 · 滚动与 resize 防抖节流 -----

【角色】你是前端交互性能工程师。

【任务】滚动/resize 频繁触发卡顿：
1. 防抖(只执行最后一次)与节流(固定频率)区别与场景。
2. requestAnimationFrame 节流滚动，比 setTimeout 更贴帧。
3. IntersectionObserver 替代 scroll 判断元素进入视口。
4. ResizeObserver 替代 window resize 监听元素尺寸。
5. passive:true 提升滚动。
6. 卸载移除监听。

【输出格式】
- debounce 与 throttle 的 TS 实现。
- rAF 节流与 IntersectionObserver 示例。
- passive 为何提升流畅度。

----- 第 59 条 · 内存泄漏排查与清理 -----

【角色】你是内存与生命周期专家。

【任务】SPA 切换页面内存持续增长：
1. 常见泄漏：未清理 setInterval/setTimeout、未移除事件监听、未取消订阅(WebSocket/EventEmitter)、闭包持有大对象、分离DOM、控制台打印大对象、Map/Set 只增不删。
2. Chrome DevTools Memory：堆快照对比、Allocation timeline、Detached DOM。
3. useEffect 清理函数中清除；Vue beforeUnmount。
4. "挂载订阅、卸载取消"标准模板。
5. 避免全局挂数据。

【输出格式】
- 6种泄漏与修复代码。
- React/Vue 清理模板。
- 堆快照对比步骤。

----- 第 60 条 · Web Worker 卸载密集计算 -----

【角色】你是前端多线程与流畅度工程师。

【任务】解析5MB JSON 并复杂聚合，UI 卡顿1-2秒：
1. 解释 JS 单线程与长任务阻塞主线程。
2. 迁移到 Web Worker：主线程 postMessage，Worker 算完 postMessage。
3. Transferable Objects(ArrayBuffer) 转移所有权避免克隆。
4. Worker 与主线程代码含错误处理与 terminate。
5. Comlink 简化通信示例。
6. Worker 限制(不能访问 DOM/window)。

【输出格式】
- worker.js 与 main.js。
- Comlink 版示例。
- 何类任务适合/不适合 Worker。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【🔤 正则表达式】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

----- 第 61 条 · 邮箱校验正则 -----

【角色】你是正则工程师，注重正确性与可维护性。

【任务】编写实用邮箱校验正则（不追求 RFC 5322 完全合规）：
1. 本地部分允许字母数字与 ._%-（不能以.开头/结尾、不能连续..）；@ 后域名含字母数字连字符，点分隔，TLD 至少2字母。
2. 给出正则与每部分注释。
3. 说明正则只做基础校验，最终靠验证邮件。
4. JS 与 Python 两版写法。
5. 合法/非法用例各5个。

【输出格式】
- 带注释正则。
- JS/Python 校验函数。
- 测试用例表。

----- 第 62 条 · 中国大陆手机号校验 -----

【角色】你是熟悉国内号段的正则工程师。

【任务】编写中国大陆手机号正则：
1. 11位数字，1开头。
2. 第二位 3-9（覆盖13x-19x）。
3. 严格版 ^1[3-9]\d{9}$ 与文本提取版。
4. 不要写死 1[358] 过时规则。
5. 支持 +86/0086 前缀与空格横线的清洗+校验函数。

【输出格式】
- 正则与清洗函数(先去非数字再校验)。
- 合法/非法用例。
- 清洗后再校验更易维护。

----- 第 63 条 · 从文本提取 URL -----

【角色】你是文本处理工程师。

【任务】从文本提取所有 URL：
1. 支持 http/https/ftp，也支持无协议 www.。
2. 正确处理 URL 后标点(句号逗号括号)不包含进 URL。
3. 支持域名/端口/路径/查询/锚点。
4. JS matchAll 示例。
5. 给严格"必须含协议"版用于安全场景。
6. 复杂情况用 new URL()。

【输出格式】
- 正则与提取代码。
- 测试文本与结果。
- 宽松 vs 严格版用途。

----- 第 64 条 · 密码强度校验正则 -----

【角色】你是表单校验工程师。

【任务】正则校验密码：至少8位，含大写、小写、数字、特殊字符(!@#$%^&*)：
1. 多个正向先行断言 (?=.*[a-z]) 组合。
2. 正则与每部分解释。
3. 返回强度等级(弱/中/强)而非布尔：≥12且四类为强，8-11且三类为中。
4. 建议多条小正则分别检测给针对性提示。

【输出格式】
- 组合断言正则。
- checkPasswordStrength 函数。
- 测试用例与预期。

----- 第 65 条 · 提取 CSS 颜色值(hex/rgb/rgba) -----

【角色】你是文本与样式处理工程师。

【任务】从 CSS 提取颜色：
1. hex：#fff/#ffffff/#ffffffff(8位alpha)。
2. rgb/rgba：rgb(255,0,0)、rgba(255,0,0,.5)，允许空格。
3. 命名捕获组。
4. JS matchAll 提取。
5. hex 简写展开6位函数。

【输出格式】
- 命名捕获组正则。
- 提取代码与展开函数。
- 测试用例。

----- 第 66 条 · 日期格式校验与提取 -----

【角色】你是日期处理工程师。

【任务】正则匹配校验日期：
1. YYYY-MM-DD，捕获组取年月日。
2. 正则基础范围：月份01-12、日期01-31，但无法准确判大小月闰年。
3. 两阶段：正则初步匹配 + 代码 Date/库真实校验。
4. 给 YYYY/MM/DD 与 DD-MM-YYYY 版。
5. 不要用正则做日期运算。

【输出格式】
- 正则与两阶段校验函数(JS/Python)。
- 合法/非法用例(含2024-02-29与2023-02-29)。
- 正则边界说明。

----- 第 67 条 · IPv4 地址校验 -----

【角色】你是网络数据处理工程师。

【任务】正则匹配 IPv4：
1. 四段 0-255 数字点分。
2. 不匹配256、01前导零、空段。
3. 两版：冗长准确0-255匹配；简单 \d{1,3} 后代码判范围(推荐)。
4. 从日志提取 IP 版(带边界)。
5. IPv6 用库(ip-address)。

【输出格式】
- 准确 IPv4 正则与注释。
- 粗匹配+代码校验函数。
- 合法/非法用例。

----- 第 68 条 · 去除/提取 HTML 标签 -----

【角色】你是文本处理工程师，警惕正则解析 HTML 局限。

【任务】
1. stripHtml(html) 去标签得纯文本(正则 /]*>/g)，处理 &nbsp; 等实体。
2. extractLinks(html) 提取所有 。
3. 警告：正则不能可靠解析任意 HTML(嵌套/注释/属性中>/畸形)，生产用 DOMParser/cheerio/BeautifulSoup。
4. 安全消毒必须用 DOMPurify。

【输出格式】
- stripHtml 与 extractLinks JS 实现。
- DOMParser/cheerio 稳妥替代。
- 3个失败案例。

----- 第 69 条 · 驼峰/短横线/下划线互转 -----

【角色】你是命名转换工程师。

【任务】实现三组互转：
1. camelCase  kebab-case。
2. camelCase  snake_case。
3. kebab-case  snake_case。
4. 正则+replace，正确处理连续大写(HTMLElement -> html-element，不要变成 h-t-m-l-element)。
5. TS 实现。
6. 边界用例(空串/全大写/已有分隔符)。

【输出格式】
- 三个转换函数。
- 连续大写处理正则思路。
- 测试用例与预期。

----- 第 70 条 · 数字千分位格式化 -----

【角色】你是数字格式化工程师。

【任务】正则给数字加千分位：
1. 整数部分从右每3位加逗号(1234567 -> 1,234,567)。
2. 小数不变(1234567.89 -> 1,234,567.89)。
3. 负数处理。
4. 正则 /\B(?=(\d{3})+(?!\d))/g 解释。
5. 生产推荐 Intl.NumberFormat/toLocaleString，正则适合无依赖。

【输出格式】
- 正则版与 Intl 版函数。
- 逐符号解释正则。
- 测试用例。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【🗄️ SQL查询】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

----- 第 71 条 · 查找重复记录 -----

【角色】你是 SQL 工程师，熟悉数据清洗。

【任务】在 users 表找重复 email：
1. GROUP BY email HAVING COUNT(*)>1 找重复邮箱及次数。
2. 查看完整重复行用 ROW_NUMBER() OVER(PARTITION BY email ORDER BY id)。
3. 区分完全重复与业务键重复。
4. MySQL 8+/PostgreSQL 窗口函数写法。

【输出格式】
- GROUP BY 版与窗口函数版 SQL。
- 适用场景说明。
- 建唯一索引防未来重复 DDL。

----- 第 72 条 · 删除重复保留最小 ID -----

【角色】你是数据清洗 SQL 工程师。

【任务】删除 users 重复 email，每邮箱保留最小 id：
1. 自连接：DELETE u1 FROM users u1 JOIN users u2 ON u1.email=u2.email AND u1.id>u2.id。
2. 窗口函数：DELETE WHERE id IN (SELECT id FROM (SELECT id,ROW_NUMBER() OVER(PARTITION BY email ORDER BY id) rn FROM users) t WHERE rn>1)。
3. 删除前先 SELECT 验证，事务中执行。
4. NULL email 处理。
5. 删除后加唯一索引。

【输出格式】
- 两种删除 SQL(标注数据库)。
- 事务包裹与先查后删流程。
- 唯一索引 DDL。

----- 第 73 条 · 高效分页(深分页优化) -----

【角色】你是 SQL 性能工程师。

【任务】实现分页并优化深分页：
1. 基础 LIMIT ?,?，page 从1。
2. 深分页问题：OFFSET 100000 仍扫描丢弃前10万行。
3. 游标 seek 分页：WHERE (created_at,id)<(?,?) ORDER BY created_at DESC,id DESC LIMIT ?，用上一页末条定位。
4. 游标分页不能跳页但性能稳定，适合无限滚动。
5. 总数查询与缓存。

【输出格式】
- LIMIT/OFFSET 版与游标版 SQL。
- 解释需唯一排序字段(id 做 tie-breaker)。
- 应用层代码示例。

----- 第 74 条 · 每组 Top N 记录 -----

【角色】你是熟悉窗口函数的 SQL 工程师。

【任务】查每部门工资最高3名员工：
1. ROW_NUMBER()/RANK()/DENSE_RANK() OVER(PARTITION BY dept_id ORDER BY salary DESC)。
2. 三者区别：ROW_NUMBER 强行编号、RANK 并列跳号、DENSE_RANK 并列不跳号。
3. 外层过滤 rn<=3。
4. 并列第3用 RANK 可能多于3人。
5. MySQL 8+/PG 写法，MySQL 5.7 用户变量老式写法。

【输出格式】
- 三种窗口函数 SQL 与结果差异。
- 示例数据与结果。
- PARTITION BY 与 GROUP BY 区别。

----- 第 75 条 · 累计求和(Running Total) -----

【角色】你是熟悉窗口函数的数据分析师。

【任务】计算每日销售额累计：
1. 表 sales(date,amount)，查每天 amount 与月初至今 running_total。
2. SUM(amount) OVER(ORDER BY date ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)。
3. 按月分区 PARTITION BY DATE_FORMAT(date,'%Y-%m')。
4. RANGE 与 ROWS 区别(重复日期时不同)。

【输出格式】
- 累计求和 SQL(含按月分区)。
- ROWS 与 RANGE 帧区别。
- 示例输出表格。

----- 第 76 条 · 行转列(Pivot) -----

【角色】你是 SQL 数据分析工程师。

【任务】把每月销售额从行转列：
1. 条件聚合：SUM(CASE WHEN MONTH(date)=1 THEN amount END) AS Jan。
2. 12个月完整 SQL。
3. PostgreSQL 用 FILTER(WHERE...)。
4. 月份动态不固定需动态 SQL(PREPARE/存储过程)。
5. 反向列转行用 UNION ALL/UNPIVOT。

【输出格式】
- CASE 版与 FILTER 版。
- MySQL PREPARE 动态列思路。
- BI 工具透视更方便。

----- 第 77 条 · 递归 CTE 查树形结构 -----

【角色】你是熟悉递归查询的 SQL 工程师。

【任务】表 categories(id,name,parent_id) 树形分类：
1. 某分类及所有后代(向下递归)。
2. 某分类及所有祖先(向上递归)。
3. WITH RECURSIVE(MySQL 8+/PG)。
4. 计算 depth 与 path(CONCAT)。
5. 防无限循环：深度限制或检测循环。

【输出格式】
- 向下与向上递归两条 SQL。
- 锚点成员与递归成员解释。
- CEO 下组织树示例。

----- 第 78 条 · 同比/环比增长 -----

【角色】你是熟悉时间序列对比的数据分析师。

【任务】表 sales(month,amount)，计算每月：
1. 同比增长率(与去年同月)：(本月-去年同月)/去年同月*100。
2. 环比(与上月)。
3. LAG(amount,12) 取去年同月，LAG(amount) 取上月。
4. NULL 致除零用 NULLIF 与 COALESCE。
5. ROUND 两位小数。

【输出格式】
- 含同比环比完整 SQL。
- LAG 偏移参数解释。
- 除零与缺失处理。

----- 第 79 条 · 找出缺失记录(NOT IN/NOT EXISTS/LEFT JOIN) -----

【角色】你是关注正确性与性能的 SQL 工程师。

【任务】找"从未下单的用户"：
1. 三种写法：NOT IN 子查询、NOT EXISTS 相关子查询、LEFT JOIN...WHERE orders.id IS NULL。
2. NOT IN 陷阱：子查询含 NULL 则 NOT IN 返回空(三值逻辑)，推荐 NOT EXISTS。
3. 性能：NOT EXISTS 与 LEFT JOIN 通常优化为反连接。
4. 进阶：递归 CTE 生成日期序列再 LEFT JOIN 找连续登录缺失日期。

【输出格式】
- 三种写法 SQL。
- 示例数据演示 NOT IN 含 NULL 错误。
- 推荐结论。

----- 第 80 条 · 移动平均值(Moving Average) -----

【角色】你是熟悉窗口帧的数据分析师。

【任务】计算7日移动平均：
1. 表 quotes(date,price)，AVG(price) OVER(ORDER BY date ROWS BETWEEN 6 PRECEDING AND CURRENT ROW)。
2. 前6天不足时 AVG 基于已有行；要求满7天则外层 COUNT>=7 过滤。
3. SMA vs EMA(EMA 需递归 CTE 或应用层)。
4. 按股票代码 PARTITION BY symbol。

【输出格式】
- 7日移动平均 SQL(含分区)。
- 6 PRECEDING 帧含义。
- 不足窗口大小的处理。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【📝 文档生成】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

----- 第 81 条 · 为函数生成 JSDoc 注释 -----

【角色】你是技术文档工程师，熟悉 JSDoc/TSDoc。

【任务】为函数生成规范 JSDoc：
function debounce(fn, delay, immediate) { ... }
1. @description 一句话功能。
2. 每 @param 带类型/名/描述，可选参数 {Type}[name=default]。
3. @returns 类型与含义。
4. @example 至少一个示例。
5. 抛异常加 @throws。
6. TS 项目用 TSDoc 避免重复类型。

【输出格式】
- 带 JSDoc 函数代码。
- @template 泛型示例。
- 说明纯 JS 中类型可被编辑器利用。

----- 第 82 条 · 生成项目 README -----

【角色】你是开发者关系工程师，擅长写 README。

【任务】为项目生成完整 README.md：
1. 项目名+一句话简介+徽章。
2. 功能特性(3-5条)。
3. 快速开始：环境要求、安装、最小示例。
4. 用法：分场景完整示例。
5. API 文档：方法签名表格。
6. 配置项表格。
7. FAQ。
8. 贡献指南、许可证。
9. 给"轻量级 HTTP 请求库"实例。

【输出格式】
- 完整 README.md。
- 代码块标注语言。
- 3分钟跑起来验收标准。

----- 第 83 条 · 生成 OpenAPI/Swagger 接口文档 -----

【角色】你是 API 设计工程师，熟悉 OpenAPI 3.0。

【任务】为用户管理模块写 OpenAPI 3.0 YAML：
1. openapi/info/servers。
2. paths：GET /users(分页+200/401)、POST /users(请求体+201/422)、GET/PUT/DELETE /users/{id}。
3. components/schemas 定义 User/UserCreate/Error/Pagination。
4. 每接口带 summary/description/tags。
5. 可粘贴 Swagger Editor。

【输出格式】
- 完整 openapi.yaml。
- swagger-ui-express 挂载说明。
- redocly 校验命令。

----- 第 84 条 · 生成 CHANGELOG(Keep a Changelog) -----

【角色】你是发布工程师，遵循 Keep a Changelog 与语义化版本。

【任务】根据提交记录生成 CHANGELOG：
- feat: 新增批量导出 CSV
- fix: 修复登录页 Safari 样式错位
- feat: 用户列表按角色筛选
- perf: 订单查询性能提升40%
- BREAKING CHANGE: /api/v1/users 返回结构变更
- docs: 更新部署文档
1. 分组 Added/Changed/Deprecated/Removed/Fixed/Security。
2. 版本 2.0.0(有 BREAKING CHANGE)，日期今天。
3. BREAKING CHANGE 高亮并说明迁移。
4. chore 杂项可不写。

【输出格式】
- 格式化 CHANGELOG.md。
- MAJOR/MINOR/PATCH 规则。
- 推荐 commitlint+standard-version。

----- 第 85 条 · 为复杂代码添加解释性注释 -----

【角色】你是注重"为什么"而非"做什么"的工程师。

【任务】为复杂/反直觉代码(位运算权限或浏览器 bug hack)加注释：
1. 注释解释"为什么"(业务原因/外部约束/性能权衡)，不复述代码。
2. hack 标注来源链接与背景，便于未来验证移除。
3. 用 TODO/FIXME/HACK 规范标签。
4. 不给显而易见代码加噪音注释。
5. 判断准则：代码表达"做什么"，注释表达"为什么"。

【输出格式】
- 带高质量注释代码示例。
- 坏注释 vs 好注释对比。
- 4类值得写注释的情况。

----- 第 86 条 · 生成 TypeDoc 文档 -----

【角色】你是 TypeScript 文档工程师。

【任务】为 TS 工具库配置生成 TypeDoc：
1. TSDoc 注释描述模块/类/方法/参数/返回/示例。
2. typedoc.json：入口、输出目录、主题、排除 private/protected、readme。
3. package.json docs 脚本 typedoc。
4. mathUtils.ts(clamp/lerp/sum)演示注释。
5. @category/@group 组织 API。
6. 发布 GitHub Pages 步骤。

【输出格式】
- mathUtils.ts 带 TSDoc。
- typedoc.json 与 package.json 脚本。
- 本地预览与部署命令。

----- 第 87 条 · 用 Mermaid 画架构/流程图 -----

【角色】你是技术文档可视化工程师。

【任务】用 Mermaid 绘制(可在 GitHub/Notion/语雀渲染)：
1. 系统架构图：客户端→CDN→负载均衡→应用服务器(多实例)→Redis/DB，flowchart LR。
2. 时序图：下单流程(用户/前端/订单/库存/支付消息顺序)。
3. 状态图：订单状态流转(待支付→已支付→已发货→已完成，含取消)。
4. ER 图：用户-订单-商品关系。

【输出格式】
- 4段 Mermaid 代码。
- 渲染位置说明。
- 易读小技巧(subgraph/方向/注释)。

----- 第 88 条 · 生成贡献指南 CONTRIBUTING.md -----

【角色】你是开源社区维护者。

【任务】为开源项目生成 CONTRIBUTING.md：
1. 行为准则链接。
2. 开发环境：版本、包管理器、装依赖、测试、lint。
3. 目录结构。
4. 提交规范：Conventional Commits。
5. PR 流程：fork→分支命名→提交→推送→PR模板→CI通过。
6. Issue 模板(bug/功能)。
7. 审查与合并规则。

【输出格式】
- CONTRIBUTING.md 全文。
- commit message 示例。
- PR/Issue 模板放 .github/。

----- 第 89 条 · 为 Python 函数写 docstring(Google 风格) -----

【角色】你是 Python 文档工程师，遵循 Google Python Style Guide。

【任务】为函数写 Google 风格 docstring：
def fetch_orders(session, user_id, status=None, page=1, size=20):
    ...
1. 一行摘要+详细说明。
2. Args：参数名/类型/描述。
3. Returns：类型与结构(如 List[dict])。
4. Raises：异常与条件。
5. Examples：doctest 示例。
6. 与 Sphinx 兼容，启用 napoleon。

【输出格式】
- 带 Google docstring 函数。
- NumPy 风格对比简述。
- 运行 doctest 命令。

----- 第 90 条 · 生成错误码文档 -----

【角色】你是 API 文档工程师。

【任务】设计错误码文档：
1. 统一错误结构 { code, message, details?, traceId }。
2. 分段：10xxx 通用、20xxx 用户、30xxx 订单、40xxx 支付。
3. 表格列至少15个：错误码、HTTP状态、含义、触发场景、客户端建议。
4. 代码中为常量/枚举。
5. TS 错误码枚举定义。
6. traceId 贯穿日志与返回。

【输出格式】
- Markdown 错误码表。
- ErrorCode 枚举与 AppError 类代码。
- 客户端按 code 友好提示建议。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【🌿 Git工作流】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

----- 第 91 条 · 编写规范 Conventional Commit -----

【角色】你是 Git 工作流工程师，遵循 Conventional Commits。

【任务】根据改动生成 commit message：
- 新增用户头像上传
- 修复订单列表分页第0页 bug
- 破坏性变更：删除废弃 /api/v1/login
1. 格式 (): ，type 为 feat/fix/docs/style/refactor/perf/test/chore/ci。
2. subject 祈使句、首字母小写、不加句号、≤50字符。
3. body 说明"是什么/为什么"，每行≤72字符。
4. BREAKING CHANGE 在 footer 说明迁移。
5. 关联 issue 用 Closes #123。

【输出格式】
- 三条完整 commit message(含 body/footer)。
- 为何用祈使句。
- 推荐 commitlint+husky。

----- 第 92 条 · 交互式 rebase 合并提交(squash/fixup) -----

【角色】你是 Git 历史整理工程师。

【任务】特性分支有5个琐碎提交(wip/fix typo/调试)，合并前整理为1-2个干净提交：
1. git rebase -i HEAD~5。
2. 解释 pick/squash/fixup/reword/drop/edit。
3. 后续提交 fixup 到第一个，保留首个 message。
4. 需改 message 用 reword。
5. 冲突解决后 git add→git rebase --continue；放弃 --abort。
6. 已推送分支用 git push --force-with-lease(比 --force 安全)。

【输出格式】
- rebase todo 前后对比。
- 完整命令流程。
- 公共分支不要 rebase 原因。

----- 第 93 条 · 解决合并冲突 -----

【角色】你是 Git 冲突调解工程师。

【任务】git merge 出现冲突：
1. 解释 >>>>>> 含义(HEAD vs 被合并分支)。
2. 流程：git status→编辑选择保留并删标记→git add→git commit(--continue)。
3. "双方改同一行"与"一方改一方删文件"分别处理。
4. 推荐 VS Code/IntelliJ 三向合并或 git mergetool。
5. 示例冲突片段与解决结果。
6. git merge --abort 回到合并前。

【输出格式】
- 冲突文件示例与解决后代码。
- 完整命令序列。
- 3条减少冲突团队实践。

----- 第 94 条 · git bisect 定位引入 bug 的提交 -----

【角色】你是 Git 排障工程师，擅长二分查找。

【任务】v1.0 正常、v2.0 异常，中间200提交，定位首个坏提交：
1. git bisect start。
2. git bisect bad 标记当前，git bisect good v1.0。
3. Git 自动 checkout 中间提交，测试后 good/bad 标记直到定位。
4. 自动化：git bisect run ，退出码0=good/1=bad/125=skip。
5. 定位后 git bisect reset。
6. npm test 作测试脚本示例。

【输出格式】
- 手动 bisect 命令序列。
- bisect run 自动化示例。
- 要求每提交可独立构建/测试。

----- 第 95 条 · cherry-pick 选择性合并提交 -----

【角色】你是 Git 分支管理工程师。

【任务】develop 上一个 bugfix 需紧急合入 main 发布，但 develop 有未完成功能：
1. git cherry-pick  单独应用该提交。
2. 多个：A B C 或区间 A..B。
3. 冲突解决后 --continue；放弃 --abort。
4. -x 追加来源哈希便于追溯。
5. -n/--no-commit 只应用不提交。
6. cherry-pick 产生新哈希；长期维护分支应 merge 避免重复。

【输出格式】
- 完整 cherry-pick 流程。
- -x/-n 示例。
- 对比 cherry-pick 与 merge 场景。

----- 第 96 条 · reset vs revert 选择 -----

【角色】你是 Git 历史安全工程师。

【任务】错误提交已推送远程需撤销，区分 reset 与 revert：
1. git reset --soft：回退指针保留改动在暂存区(本地未推送整理)。
2. --mixed(默认)：保留改动在工作区不暂存。
3. --hard：彻底丢弃提交与改动(危险)。
4. git revert：生成新反向提交，不改写历史(适合已推送公共分支)。
5. 已推送共享分支绝不用 reset --hard+force push。
6. 撤销最近一次提交两种正确做法。

【输出格式】
- 表格对比 reset 三模式与 revert。
- 本地未推送 vs 已推送场景示例。
- git reflog 找回误 reset。

----- 第 97 条 · stash 临时保存工作区 -----

【角色】你是 Git 日常工作流工程师。

【任务】feature 开发到一半需紧急切 main 修 bug，改动不能提交：
1. git stash push -m "wip: 用户列表"。
2. -u/--include-untracked 保存未跟踪文件。
3. git stash list 查看。
4. git stash pop 恢复并删除；apply 恢复但保留。
5. git stash show -p stash@{0} 查看具体改动。
6. stash 不包含 ignored 文件；pop 冲突需手动解决。
7. stash 只是临时，长期未完成应提交临时分支。

【输出格式】
- 完整 stash 工作流命令。
- apply vs pop、含未跟踪示例。
- 2个注意事项。

----- 第 98 条 · GitFlow 分支策略 -----

【角色】你是 Git 流程架构师，熟悉 GitFlow 与 GitHub Flow。

【任务】为有版本发布周期团队设计 GitFlow：
1. 长期分支：main(生产打tag)、develop(集成)。
2. 临时分支：feature/*(develop切出合回develop)、release/*(发布准备只修bug，合入main与develop并打tag)、hotfix/*(main切出修线上，合入main与develop)。
3. 命名规范与生命周期。
4. 对比 GitHub Flow(main+feature持续部署)与 Trunk-Based。
5. feature 到发布完整命令流程。

【输出格式】
- GitFlow 分支图(文字版)与合并规则。
- feature→release→hotfix 命令示例。
- GitFlow vs GitHub Flow 选型建议。

----- 第 99 条 · 定位 bug 引入提交(log/blame/二分) -----

【角色】你是 Git 考古与排障工程师。

【任务】一行代码疑似致 bug，找是谁/哪个提交/为何引入：
1. git blame  查看每行最后修改提交/作者/时间。
2. git blame -L 40,60 只看行范围。
3. git show  查完整改动与 message。
4. git log -S "关键字"(pickaxe) 找引入或删除字符串的提交。
5. git log -G "正则" 按正则搜改动。
6. git log --oneline -p  查历史逐次改动。
7. 结合 git bisect 二分定位。
8. 定位后应理解原因而非追责。

【输出格式】
- blame/show/log -S/log -G 命令示例。
- "定位某函数被删除提交"完整流程。
- blame -w 忽略空白变更。

----- 第 100 条 · 清理已合并的本地与远程分支 -----

【角色】你是 Git 仓库卫生工程师。

【任务】大量已合并 feature 分支堆积，需清理：
1. git branch --merged 查已合并本地分支。
2. git branch -d  删除(-d 拒绝未合并，安全；-D 强制)。
3. 一键清理：git branch --merged | grep -v '^\*\|main\|develop' | xargs git branch -d。
4. 删远程：git push origin --delete 。
5. 清理远程已删分支的本地引用：git fetch --prune。
6. 删除前确认分支已合入目标分支。

【输出格式】
- 本地与远程清理命令序列。
- --prune 示例。
- 建议定期清理与保护 main/develop 规则。

==============================================
  共 100 条 Prompt，覆盖 10 大场景
  如需帮助：微信 18192939315 / 邮箱 haolinsong32@gmail.com
  SUKA.net · A老板的AI服务中心
==============================================