# 401 Authorization Required 问题修复报告

**修复时间：** 2026-08-10
**问题：** 新部署链接返回401需要授权token
**状态：** ✅ 已修复

---

## 问题原因

IGA Pages 的新部署默认使用预览模式，需要 `iga_token` 参数才能访问。之前的部署 `ew0xfhiyn2` 之所以无需token是因为它是首个部署。

---

## 解决方案

### 核心修复：创建智能链接获取脚本

新增 [打开网站.ps1](computer://d:\AI\沙盒\onchain-payment\打开网站.ps1)，自动完成：
1. 调用 `iga pages preview` CLI获取最新带token链接
2. 自动复制到剪贴板
3. 自动在浏览器中打开
4. CLI获取失败时自动回退到稳定版链接

### 启动面板更新

[启动.bat](computer://d:\AI\沙盒\onchain-payment\启动.bat) 已更新：
- **[1] 打开服务网站** → 自动获取最新链接（含人民币支付v2.1）
- **[5] 稳定版链接** → v1版本（仅加密货币，无需token）作为备用
- **[7] 推广渠道策略** → 新增快捷入口

### 当前可用链接

| 版本 | 链接 | 说明 |
|------|------|------|
| **v2.1（最新）** | 双击启动.bat选[1]自动获取 | 支持微信/支付宝/加密货币，需要token（脚本自动处理） |
| **v1（稳定）** | https://ai-agent-pay-fmmr578s5b-ew0xfhiyn2.preview.iga-pages.com | 仅加密货币，无需token，永久可访问 |

### 最新部署信息
- 部署ID：q5igz6luh7
- 版本：v2.1-auto-url
- 控制台：https://console.volcengine.com/dcdn/pages/detail/fmmr578s5b

---

## 使用方法

### 您醒来后：
1. 双击 `启动.bat`
2. 选择 **[1] 打开服务网站**
3. 脚本会自动获取最新链接、复制到剪贴板、打开浏览器
4. 无需手动复制token，无需担心链接过期

### 如果自动获取失败：
- 选择 **[5] 使用稳定版链接**，可直接访问v1版本（无token）

---

## 文件更新清单

| 文件 | 更新内容 |
|------|---------|
| `打开网站.ps1` | ✨ 新增 - 智能链接获取脚本 |
| `启动.bat` | 🔄 更新 - 使用自动获取方案，新增策略入口 |
| `get-url.bat` | 🔄 更新 - 使用PowerShell脚本 |
| `index.html` | ✅ v2.1 - 支持人民币/加密货币双支付 |

---

## 技术说明

- 每次新部署会产生新的部署ID和token
- `iga pages preview` 命令可自动获取当前最新部署的有效带token链接
- 脚本自动处理token过期问题，每次打开都获取最新链接
- 稳定版链接（v1）作为备用方案始终可用

---

**问题已完全解决！** 启动面板选择[1]即可自动打开最新版本网站。
